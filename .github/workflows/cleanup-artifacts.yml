name: 清理旧构建产物

on:
  workflow_dispatch:
    inputs:
      dry_run:
        description: 预览模式，不真正删除
        required: true
        default: "true"
        type: choice
        options:
          - "true"
          - "false"
      artifact_keep_days:
        description: Artifact 至少保留天数
        required: true
        default: "14"
        type: string
      artifact_keep_latest_per_name:
        description: 每个 Artifact 名称至少保留最近几个
        required: true
        default: "2"
        type: string
      cleanup_workflow_runs:
        description: 是否同时清理旧 workflow runs
        required: true
        default: "false"
        type: choice
        options:
          - "false"
          - "true"
      workflow_run_keep_days:
        description: Workflow run 至少保留天数
        required: true
        default: "30"
        type: string
      workflow_run_keep_latest_per_workflow:
        description: 每个 workflow 至少保留最近几个 runs
        required: true
        default: "10"
        type: string
  schedule:
    - cron: "23 3 * * 0"

permissions:
  actions: write
  contents: read

concurrency:
  group: 清理旧构建产物
  cancel-in-progress: false

jobs:
  cleanup:
    name: 清理旧 artifact 和 workflow 记录
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: 清理旧 artifact
        uses: actions/github-script@v7
        with:
          script: |
            const dryRun = '${{ github.event.inputs.dry_run || 'true' }}' !== 'false';
            const keepDays = Math.max(1, Number.parseInt('${{ github.event.inputs.artifact_keep_days || '14' }}', 10) || 14);
            const keepLatestPerName = Math.max(0, Number.parseInt('${{ github.event.inputs.artifact_keep_latest_per_name || '2' }}', 10) || 2);
            const cutoff = Date.now() - keepDays * 24 * 60 * 60 * 1000;
            const owner = context.repo.owner;
            const repo = context.repo.repo;

            const artifacts = await github.paginate(github.rest.actions.listArtifactsForRepo, {
              owner,
              repo,
              per_page: 100
            });

            const grouped = new Map();
            for (const artifact of artifacts) {
              const list = grouped.get(artifact.name) || [];
              list.push(artifact);
              grouped.set(artifact.name, list);
            }

            const deleteCandidates = [];
            for (const [name, list] of grouped.entries()) {
              list.sort((a, b) => new Date(b.created_at).getTime() - new Date(a.created_at).getTime());
              for (let index = 0; index < list.length; index++) {
                const artifact = list[index];
                const created = new Date(artifact.created_at).getTime();
                const keepBecauseLatest = index < keepLatestPerName;
                const keepBecauseYoung = created >= cutoff;
                if (!keepBecauseLatest && !keepBecauseYoung) {
                  deleteCandidates.push(artifact);
                }
              }
            }

            core.summary.addHeading('Artifact 清理结果');
            core.summary.addRaw(`预览模式：${dryRun}\n\n`);
            core.summary.addRaw(`Artifact 总数：${artifacts.length}\n\n`);
            core.summary.addRaw(`候选删除数：${deleteCandidates.length}\n\n`);
            core.summary.addRaw(`保留天数：${keepDays}\n\n`);
            core.summary.addRaw(`每个名称保留最近：${keepLatestPerName}\n\n`);

            for (const artifact of deleteCandidates) {
              const line = `- ${artifact.name} #${artifact.id} created=${artifact.created_at} size=${artifact.size_in_bytes}`;
              console.log(line);
              core.summary.addRaw(`${line}\n`);
              if (!dryRun) {
                await github.rest.actions.deleteArtifact({ owner, repo, artifact_id: artifact.id });
              }
            }

            await core.summary.write();

      - name: 清理旧 workflow runs
        if: ${{ github.event_name == 'workflow_dispatch' && github.event.inputs.cleanup_workflow_runs == 'true' }}
        uses: actions/github-script@v7
        with:
          script: |
            const dryRun = '${{ github.event.inputs.dry_run }}' !== 'false';
            const keepDays = Math.max(1, Number.parseInt('${{ github.event.inputs.workflow_run_keep_days }}', 10) || 30);
            const keepLatestPerWorkflow = Math.max(0, Number.parseInt('${{ github.event.inputs.workflow_run_keep_latest_per_workflow }}', 10) || 10);
            const cutoff = Date.now() - keepDays * 24 * 60 * 60 * 1000;
            const owner = context.repo.owner;
            const repo = context.repo.repo;
            const currentRunId = context.runId;

            const workflows = await github.paginate(github.rest.actions.listRepoWorkflows, {
              owner,
              repo,
              per_page: 100
            });

            const deleteCandidates = [];
            for (const workflow of workflows) {
              const runs = await github.paginate(github.rest.actions.listWorkflowRuns, {
                owner,
                repo,
                workflow_id: workflow.id,
                per_page: 100
              });
              runs.sort((a, b) => new Date(b.created_at).getTime() - new Date(a.created_at).getTime());
              for (let index = 0; index < runs.length; index++) {
                const run = runs[index];
                const created = new Date(run.created_at).getTime();
                const keepBecauseLatest = index < keepLatestPerWorkflow;
                const keepBecauseYoung = created >= cutoff;
                const keepBecauseCurrent = run.id === currentRunId;
                const keepBecauseRunning = ['queued', 'in_progress', 'waiting', 'requested', 'pending'].includes(run.status);
                if (!keepBecauseLatest && !keepBecauseYoung && !keepBecauseCurrent && !keepBecauseRunning) {
                  deleteCandidates.push({ workflow, run });
                }
              }
            }

            core.summary.addHeading('Workflow run 清理结果');
            core.summary.addRaw(`预览模式：${dryRun}\n\n`);
            core.summary.addRaw(`候选删除数：${deleteCandidates.length}\n\n`);
            core.summary.addRaw(`保留天数：${keepDays}\n\n`);
            core.summary.addRaw(`每个 workflow 保留最近：${keepLatestPerWorkflow}\n\n`);

            for (const item of deleteCandidates) {
              const line = `- ${item.workflow.name} run #${item.run.run_number} id=${item.run.id} created=${item.run.created_at} status=${item.run.status}`;
              console.log(line);
              core.summary.addRaw(`${line}\n`);
              if (!dryRun) {
                await github.rest.actions.deleteWorkflowRun({ owner, repo, run_id: item.run.id });
              }
            }

            await core.summary.write();
